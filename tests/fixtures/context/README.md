Fixture for the README's context-file examples. `AGENTS.md` here is deliberately
broken in three ways — a dead `@import`, a dead link, and a leftover marker —
because those are the three findings the documented output shows.
